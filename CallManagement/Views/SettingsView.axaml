<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:CallManagement.ViewModels"
             xmlns:conv="using:CallManagement.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="700"
             x:Class="CallManagement.Views.SettingsView"
             x:DataType="vm:SettingsViewModel">
    
    <Design.DataContext>
        <vm:SettingsViewModel/>
    </Design.DataContext>
    
    <UserControl.Resources>
        <conv:BoolToOpacityConverter x:Key="BoolToOpacity"/>
        <conv:BoolToSavingTextConverter x:Key="BoolToSavingText"/>
    </UserControl.Resources>
    
    <!-- ═══════════════════════════════════════════════════════════════════════ -->
    <!-- MAIN SETTINGS CONTAINER -->
    <!-- ═══════════════════════════════════════════════════════════════════════ -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="24" Margin="32">
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- HEADER -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <StackPanel Spacing="4">
                <TextBlock Classes="heading2" Text="⚙️ Settings"/>
                <TextBlock Classes="bodySecondary" 
                           Text="Quản lý giao diện, bảo mật và Telegram Bot"/>
            </StackPanel>
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- STATUS MESSAGE -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border IsVisible="{Binding ShowStatusMessage}"
                    CornerRadius="8"
                    Padding="12">
                <Border.Background>
                    <MultiBinding Converter="{x:Static BoolConverters.Or}">
                        <Binding Path="IsStatusSuccess"/>
                    </MultiBinding>
                </Border.Background>
                <Border.Styles>
                    <Style Selector="Border[Tag=success]">
                        <Setter Property="Background" Value="{DynamicResource SuccessLightBrush}"/>
                    </Style>
                </Border.Styles>
                <Panel>
                    <!-- Success Background -->
                    <Border IsVisible="{Binding IsStatusSuccess}"
                            Background="{DynamicResource SuccessLightBrush}"
                            CornerRadius="8"
                            Margin="-12"
                            Padding="12">
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{DynamicResource SuccessTextBrush}"
                                   FontWeight="Medium"/>
                    </Border>
                    <!-- Error Background -->
                    <Border IsVisible="{Binding !IsStatusSuccess}"
                            Background="{DynamicResource ErrorLightBrush}"
                            CornerRadius="8"
                            Margin="-12"
                            Padding="12">
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{DynamicResource ErrorTextBrush}"
                                   FontWeight="Medium"/>
                    </Border>
                </Panel>
            </Border>
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- APPEARANCE SECTION (Theme) -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Classes="card" Padding="20" CornerRadius="12">
                <StackPanel Spacing="16">
                    
                    <!-- Section Header -->
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="🎨" FontSize="20"/>
                        <TextBlock Classes="heading3" Text="Appearance" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,4"/>
                    
                    <!-- Theme Selection (Coming Soon) -->
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Classes="body" Text="Theme" FontWeight="Medium"/>
                            <TextBlock Classes="caption" Text="Chọn giao diện sáng, tối hoặc theo hệ thống" 
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1"
                                   Text="Light (Default)"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                    
                    <!-- Theme Info -->
                    <Border Background="{DynamicResource NeutralLightBrush}"
                            CornerRadius="8"
                            Padding="12">
                        <StackPanel Spacing="4">
                            <TextBlock Classes="caption" FontWeight="SemiBold" Text="💡 Ghi chú"/>
                            <TextBlock Classes="caption" 
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextSecondaryBrush}">
                                <Run Text="• System: Tự động theo cài đặt hệ điều hành"/>
                                <LineBreak/>
                                <Run Text="• Thay đổi được áp dụng ngay lập tức"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- SECURITY SECTION -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Classes="card" Padding="20" CornerRadius="12">
                <StackPanel Spacing="16">
                    
                    <!-- Section Header -->
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="🔐" FontSize="20"/>
                        <TextBlock Classes="heading3" Text="Security" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,4"/>
                    
                    <!-- Unlocked State -->
                    <StackPanel IsVisible="{Binding IsUnlocked}" Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="✔️" FontSize="16" Foreground="{DynamicResource SuccessTextBrush}"/>
                            <TextBlock Text="Settings unlocked" 
                                       Classes="body"
                                       Foreground="{DynamicResource SuccessTextBrush}"
                                       FontWeight="Medium"/>
                        </StackPanel>
                        
                        <Button Classes="secondary"
                                Command="{Binding LockCommand}"
                                HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="🔒" FontSize="14"/>
                                <TextBlock Text="Lock Settings"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    
                    <!-- Locked State - Password Input -->
                    <StackPanel IsVisible="{Binding !IsUnlocked}" Spacing="12">
                        <TextBlock Classes="bodySecondary" 
                                   Text="Nhập mật khẩu để truy cập cài đặt Telegram"/>
                        
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                            <!-- Password Input -->
                            <TextBox Grid.Row="0" Grid.Column="0"
                                     PasswordChar="●"
                                     Text="{Binding PasswordInput, Mode=TwoWay}"
                                     Watermark="Mật khẩu"
                                     Classes="settingsInput"
                                     Margin="0,0,12,0"/>
                            
                            <!-- Unlock Button -->
                            <Button Grid.Row="0" Grid.Column="1"
                                    Classes="primary"
                                    Command="{Binding UnlockCommand}"
                                    MinWidth="100">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="🔓" FontSize="14"/>
                                    <TextBlock Text="Unlock"/>
                                </StackPanel>
                            </Button>
                            
                            <!-- Password Error -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                       IsVisible="{Binding ShowPasswordError}"
                                       Text="{Binding PasswordError}"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="12"
                                       Margin="0,8,0,0"/>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- TELEGRAM BOT SECTION -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Classes="card" Padding="20" CornerRadius="12"
                    Opacity="{Binding IsUnlocked, Converter={StaticResource BoolToOpacity}}">
                <Border.Styles>
                    <Style Selector="Border">
                        <Style.Setters>
                            <Setter Property="Transitions">
                                <Transitions>
                                    <DoubleTransition Property="Opacity" Duration="0:0:0.2"/>
                                </Transitions>
                            </Setter>
                        </Style.Setters>
                    </Style>
                </Border.Styles>
                
                <StackPanel Spacing="16">
                    
                    <!-- Section Header -->
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="🤖" FontSize="20"/>
                        <TextBlock Classes="heading3" Text="Telegram Bot" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,4"/>
                    
                    <!-- Locked Message -->
                    <Border IsVisible="{Binding !IsUnlocked}"
                            Background="{DynamicResource WarningLightBrush}"
                            CornerRadius="8"
                            Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="🔒" FontSize="14"/>
                            <TextBlock Text="Vui lòng unlock để truy cập cài đặt này"
                                       Foreground="{DynamicResource WarningTextBrush}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Telegram Settings Form -->
                    <StackPanel IsVisible="{Binding IsUnlocked}" Spacing="16">
                        
                        <!-- Bot Token Field -->
                        <StackPanel Spacing="6">
                            <TextBlock Classes="body" Text="Bot Token" FontWeight="Medium"/>
                            <TextBox Text="{Binding TelegramBotToken, Mode=TwoWay}"
                                     Watermark="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
                                     Classes="settingsInput"/>
                            <TextBlock IsVisible="{Binding ShowBotTokenError}"
                                       Text="{Binding BotTokenError}"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="12"/>
                        </StackPanel>
                        
                        <!-- Chat ID Field -->
                        <StackPanel Spacing="6">
                            <TextBlock Classes="body" Text="Chat ID" FontWeight="Medium"/>
                            <TextBox Text="{Binding TelegramChatId, Mode=TwoWay}"
                                     Watermark="-1001234567890"
                                     Classes="settingsInput"/>
                            <TextBlock IsVisible="{Binding ShowChatIdError}"
                                       Text="{Binding ChatIdError}"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="12"/>
                        </StackPanel>
                        
                        <!-- Help Text -->
                        <Border Background="{DynamicResource NeutralLightBrush}"
                                CornerRadius="8"
                                Padding="12">
                            <StackPanel Spacing="4">
                                <TextBlock Classes="caption" FontWeight="SemiBold" Text="💡 Hướng dẫn"/>
                                <TextBlock Classes="caption" 
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="1. Tạo bot tại @BotFather trên Telegram"/>
                                    <LineBreak/>
                                    <Run Text="2. Copy Bot Token từ BotFather"/>
                                    <LineBreak/>
                                    <Run Text="3. Lấy Chat ID từ @userinfobot hoặc chat với bot"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                        
                        <!-- Action Buttons -->
                        <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,8,0,0">
                            <Button Grid.Column="1"
                                    Classes="secondary"
                                    Command="{Binding ClearSensitiveDataCommand}"
                                    Margin="0,0,12,0"
                                    ToolTip.Tip="Xóa toàn bộ cài đặt Telegram">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="🗑️" FontSize="14"/>
                                    <TextBlock Text="Xóa"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Grid.Column="2"
                                    Classes="primary"
                                    Command="{Binding SaveSettingsCommand}"
                                    IsEnabled="{Binding !IsSaving}"
                                    MinWidth="120">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="💾" FontSize="14"/>
                                    <TextBlock Text="{Binding IsSaving, Converter={StaticResource BoolToSavingText}}"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- DAILY REPORT SECTION -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Classes="card" Padding="20" CornerRadius="12"
                    Opacity="{Binding IsUnlocked, Converter={StaticResource BoolToOpacity}}">
                <StackPanel Spacing="16">
                    
                    <!-- Section Header -->
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="📊" FontSize="20"/>
                        <TextBlock Classes="heading3" Text="Daily Report" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,4"/>
                    
                    <!-- Locked Message -->
                    <Border IsVisible="{Binding !IsUnlocked}"
                            Background="{DynamicResource WarningLightBrush}"
                            CornerRadius="8"
                            Padding="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="🔒" FontSize="14"/>
                            <TextBlock Text="Vui lòng unlock để truy cập cài đặt này"
                                       Foreground="{DynamicResource WarningTextBrush}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Daily Report Settings -->
                    <StackPanel IsVisible="{Binding IsUnlocked}" Spacing="16">
                        
                        <!-- Enable Auto Send Toggle -->
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Classes="body" Text="Tự động gửi Daily Report" FontWeight="Medium"/>
                                <TextBlock Classes="caption" Text="Gửi báo cáo tự động theo giờ cấu hình" 
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                            <ToggleSwitch Grid.Column="1" 
                                          IsChecked="{Binding EnableAutoSendDailyReport, Mode=TwoWay}"
                                          OnContent="Bật"
                                          OffContent="Tắt"/>
                        </Grid>
                        
                        <!-- Send Time Picker -->
                        <StackPanel Spacing="6" IsEnabled="{Binding EnableAutoSendDailyReport}">
                            <TextBlock Classes="body" Text="Giờ gửi báo cáo" FontWeight="Medium"/>
                            <Grid ColumnDefinitions="Auto,Auto,Auto,*">
                                <ComboBox Grid.Column="0"
                                          Width="80"
                                          SelectedItem="{Binding SelectedHour, Mode=TwoWay}"
                                          ItemsSource="{Binding AvailableHours}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding StringFormat={}{0:D2}}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <TextBlock Grid.Column="1" Text=":" FontSize="20" FontWeight="Bold"
                                           VerticalAlignment="Center" Margin="8,0"/>
                                <ComboBox Grid.Column="2"
                                          Width="80"
                                          SelectedItem="{Binding SelectedMinute, Mode=TwoWay}"
                                          ItemsSource="{Binding AvailableMinutes}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding StringFormat={}{0:D2}}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                        
                        <!-- Info Box -->
                        <Border Background="{DynamicResource NeutralLightBrush}"
                                CornerRadius="8"
                                Padding="12">
                            <StackPanel Spacing="4">
                                <TextBlock Classes="caption" FontWeight="SemiBold" Text="💡 Ghi chú"/>
                                <TextBlock Classes="caption" 
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="• Daily Report tổng hợp cuộc gọi trong ngày"/>
                                    <LineBreak/>
                                    <Run Text="• Chỉ gửi khi app đang chạy"/>
                                    <LineBreak/>
                                    <Run Text="• Mỗi ngày chỉ gửi 1 lần"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- FOOTER INFO -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Background="{DynamicResource NeutralLightBrush}"
                    CornerRadius="8"
                    Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Classes="caption" 
                               FontWeight="SemiBold" 
                               Text="🛡️ Thông tin bảo mật"/>
                    <TextBlock Classes="caption" 
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextSecondaryBrush}">
                        <Run Text="• Mật khẩu được mã hóa SHA-256, không lưu plain text"/>
                        <LineBreak/>
                        <Run Text="• Cài đặt Telegram được lưu cục bộ trên máy"/>
                        <LineBreak/>
                        <Run Text="• Khởi động lại app cần nhập lại mật khẩu"/>
                    </TextBlock>
                </StackPanel>
            </Border>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
